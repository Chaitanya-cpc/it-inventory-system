click>=8.0
pytest>=7.0
Flask>=2.0
Flask-WTF>=1.0  # Optional, but highly recommended for handling web forms securely
WTForms[email] # Optional, needed by Flask-WTF for specific field types
python-dotenv>=0.19 # Optional, for managing environment variables like secret keys